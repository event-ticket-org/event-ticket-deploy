# Ship the backup off the machine, but only when taking it succeeded.
#
# OnSuccess, not a second timer: there is nothing worth uploading until a backup has been taken
# AND restored into a scratch database, and a schedule of its own would cheerfully re-upload
# yesterday's copy on a night when tonight's failed - which looks exactly like working.
[Unit]
OnSuccess=offsite-sync.service
